{
    "builds": [{
        "src": "Aplicacionweb/wsgi.py",
        "use": "@ardnt/vercel-python-wsgi",
        "config": { "maxLambdaSize": "15mb" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "Aplicacionweb/wsgi.py"
        }
    ]
}